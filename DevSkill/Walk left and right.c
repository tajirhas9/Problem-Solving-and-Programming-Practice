#include <stdio.h>
int main()
{
    int i,t,x;
    long long int n[1000],k,steps=0;
    scanf("%d",&t);
    for(i=1;i<=t;i++)
    {
        scanf("%lld",&n[i]);
    }
    scanf("%d",&x);
    scanf("%lld",&k);
    for(i=x;i<;i++)
    {
        if()
    }
    return 0;
}
